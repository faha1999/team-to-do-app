/**
 * Seed data for local development.
 * TODO: populate with demo users, teams, projects, and tasks.
 */
async function main() {
  // Implement seeding logic using Prisma Client.
}

main().catch((error) => {
  console.error("Seed failed", error);
  process.exit(1);
});
